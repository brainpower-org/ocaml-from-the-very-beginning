version: "3"
services:
  ocaml:
    build: .
    image: brainpower/ocaml-from-the-very-beginning-dev
    ports:
      - "8443:8443"
    volumes:
      - ".:/repo/"
    entrypoint: /bin/bash
    command: /code-server.sh
